import React from 'react'

const LineDisplay = ({ tabs }: any) => {
  return (
    <div>
      {/* {tabs.map(tab =>
        <div key={tab.index}>
          {tab.url}
        </div>
        )} */}
    </div>
  )
}

export default LineDisplay